{
  "info": {
    "name": "Fashion Shop API - Full",
    "_postman_id": "abcd1234-5678-90ef-ghij-klmnopqrstuv",
    "description": "Full API collection for Fashion E-commerce backend (Auth, Users, Roles, Addresses, Categories, Brands, Attributes, Colors, Sizes, Products, Variants, Images, Carts, Wishlists, Orders, Payments, Shipments, Returns, Reviews, Inventory, Coupons, Promotions, Audit Logs, Webhooks)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Auth",
      "item": [
        {
          "name": "Register",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/auth/register",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"user@example.com\",\n  \"password\": \"123456\",\n  \"full_name\": \"Test User\"\n}"
            }
          }
        },
        {
          "name": "Login",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/auth/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"user@example.com\",\n  \"password\": \"123456\"\n}"
            }
          }
        },
        { "name": "Logout", "request": { "method": "POST", "url": "{{baseUrl}}/auth/logout" } },
        { "name": "Refresh Token", "request": { "method": "POST", "url": "{{baseUrl}}/auth/refresh-token" } }
      ]
    },
    {
      "name": "Users",
      "item": [
        { "name": "Get Me", "request": { "method": "GET", "url": "{{baseUrl}}/users/me" } },
        {
          "name": "Update Me",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/users/me",
            "body": { "mode": "raw", "raw": "{\n  \"full_name\": \"Updated Name\"\n}" }
          }
        },
        { "name": "Get User by ID (admin)", "request": { "method": "GET", "url": "{{baseUrl}}/users/:id" } },
        { "name": "Update User by ID (admin)", "request": { "method": "PATCH", "url": "{{baseUrl}}/users/:id" } },
        { "name": "Delete User by ID (admin)", "request": { "method": "DELETE", "url": "{{baseUrl}}/users/:id" } }
      ]
    },
    {
      "name": "Roles",
      "item": [
        { "name": "List Roles", "request": { "method": "GET", "url": "{{baseUrl}}/roles" } },
        {
          "name": "Create Role",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/roles",
            "body": { "mode": "raw", "raw": "{\n  \"code\": \"admin\",\n  \"name\": \"Administrator\"\n}" }
          }
        },
        { "name": "Update Role", "request": { "method": "PATCH", "url": "{{baseUrl}}/roles/:id" } },
        { "name": "Delete Role", "request": { "method": "DELETE", "url": "{{baseUrl}}/roles/:id" } },
        { "name": "Assign Role to User", "request": { "method": "POST", "url": "{{baseUrl}}/users/:id/roles" } }
      ]
    },
    {
      "name": "Addresses",
      "item": [
        { "name": "List My Addresses", "request": { "method": "GET", "url": "{{baseUrl}}/users/me/addresses" } },
        {
          "name": "Add Address",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/users/me/addresses",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"label\": \"Home\",\n  \"line1\": \"123 Street\",\n  \"city\": \"HCM\",\n  \"country_code\": \"VN\"\n}"
            }
          }
        },
        { "name": "Update Address", "request": { "method": "PATCH", "url": "{{baseUrl}}/users/me/addresses/:id" } },
        { "name": "Delete Address", "request": { "method": "DELETE", "url": "{{baseUrl}}/users/me/addresses/:id" } }
      ]
    },
    {
      "name": "Categories",
      "item": [
        { "name": "List Categories", "request": { "method": "GET", "url": "{{baseUrl}}/categories" } },
        { "name": "Get Category Tree", "request": { "method": "GET", "url": "{{baseUrl}}/categories/tree" } },
        { "name": "Create Category", "request": { "method": "POST", "url": "{{baseUrl}}/categories" } },
        { "name": "Update Category", "request": { "method": "PATCH", "url": "{{baseUrl}}/categories/:id" } },
        { "name": "Delete Category", "request": { "method": "DELETE", "url": "{{baseUrl}}/categories/:id" } }
      ]
    },
    {
      "name": "Brands",
      "item": [
        { "name": "List Brands", "request": { "method": "GET", "url": "{{baseUrl}}/brands" } },
        { "name": "Create Brand", "request": { "method": "POST", "url": "{{baseUrl}}/brands" } },
        { "name": "Update Brand", "request": { "method": "PATCH", "url": "{{baseUrl}}/brands/:id" } },
        { "name": "Delete Brand", "request": { "method": "DELETE", "url": "{{baseUrl}}/brands/:id" } }
      ]
    },
    {
      "name": "Attributes",
      "item": [
        { "name": "List Attributes", "request": { "method": "GET", "url": "{{baseUrl}}/attributes" } },
        { "name": "Create Attribute", "request": { "method": "POST", "url": "{{baseUrl}}/attributes" } },
        { "name": "Update Attribute", "request": { "method": "PATCH", "url": "{{baseUrl}}/attributes/:id" } },
        { "name": "Delete Attribute", "request": { "method": "DELETE", "url": "{{baseUrl}}/attributes/:id" } }
      ]
    },
    {
      "name": "Colors",
      "item": [
        { "name": "List Colors", "request": { "method": "GET", "url": "{{baseUrl}}/colors" } },
        { "name": "Create Color", "request": { "method": "POST", "url": "{{baseUrl}}/colors" } },
        { "name": "Update Color", "request": { "method": "PATCH", "url": "{{baseUrl}}/colors/:id" } },
        { "name": "Delete Color", "request": { "method": "DELETE", "url": "{{baseUrl}}/colors/:id" } }
      ]
    },
    {
      "name": "Sizes",
      "item": [
        { "name": "List Sizes", "request": { "method": "GET", "url": "{{baseUrl}}/sizes" } },
        { "name": "Create Size", "request": { "method": "POST", "url": "{{baseUrl}}/sizes" } },
        { "name": "Update Size", "request": { "method": "PATCH", "url": "{{baseUrl}}/sizes/:id" } },
        { "name": "Delete Size", "request": { "method": "DELETE", "url": "{{baseUrl}}/sizes/:id" } }
      ]
    },
    {
      "name": "Products",
      "item": [
        { "name": "List Products", "request": { "method": "GET", "url": "{{baseUrl}}/products" } },
        { "name": "Get Product by ID/Slug", "request": { "method": "GET", "url": "{{baseUrl}}/products/:id" } },
        { "name": "Create Product", "request": { "method": "POST", "url": "{{baseUrl}}/products" } },
        { "name": "Update Product", "request": { "method": "PATCH", "url": "{{baseUrl}}/products/:id" } },
        { "name": "Delete Product", "request": { "method": "DELETE", "url": "{{baseUrl}}/products/:id" } }
      ]
    },
    {
      "name": "Product Variants",
      "item": [
        { "name": "List Variants", "request": { "method": "GET", "url": "{{baseUrl}}/products/:id/variants" } },
        { "name": "Create Variant", "request": { "method": "POST", "url": "{{baseUrl}}/products/:id/variants" } },
        { "name": "Update Variant", "request": { "method": "PATCH", "url": "{{baseUrl}}/variants/:id" } },
        { "name": "Delete Variant", "request": { "method": "DELETE", "url": "{{baseUrl}}/variants/:id" } }
      ]
    },
    {
      "name": "Images",
      "item": [
        { "name": "List Product Images", "request": { "method": "GET", "url": "{{baseUrl}}/products/:id/images" } },
        { "name": "Add Product Image", "request": { "method": "POST", "url": "{{baseUrl}}/products/:id/images" } },
        { "name": "Delete Product Image", "request": { "method": "DELETE", "url": "{{baseUrl}}/products/:id/images/:imageId" } },
        { "name": "List Variant Images", "request": { "method": "GET", "url": "{{baseUrl}}/variants/:id/images" } },
        { "name": "Add Variant Image", "request": { "method": "POST", "url": "{{baseUrl}}/variants/:id/images" } },
        { "name": "Delete Variant Image", "request": { "method": "DELETE", "url": "{{baseUrl}}/variants/:id/images/:imageId" } }
      ]
    },
    {
      "name": "Carts",
      "item": [
        { "name": "Get My Cart", "request": { "method": "GET", "url": "{{baseUrl}}/carts/me" } },
        { "name": "Add Item", "request": { "method": "POST", "url": "{{baseUrl}}/carts/me/items" } },
        { "name": "Update Item", "request": { "method": "PATCH", "url": "{{baseUrl}}/carts/me/items/:id" } },
        { "name": "Remove Item", "request": { "method": "DELETE", "url": "{{baseUrl}}/carts/me/items/:id" } }
      ]
    },
    {
      "name": "Wishlists",
      "item": [
        { "name": "Get My Wishlist", "request": { "method": "GET", "url": "{{baseUrl}}/wishlists/me" } },
        { "name": "Add Item", "request": { "method": "POST", "url": "{{baseUrl}}/wishlists/me/items" } },
        { "name": "Remove Item", "request": { "method": "DELETE", "url": "{{baseUrl}}/wishlists/me/items/:id" } }
      ]
    },
    {
      "name": "Orders",
      "item": [
        { "name": "List Orders", "request": { "method": "GET", "url": "{{baseUrl}}/orders" } },
        { "name": "Get Order", "request": { "method": "GET", "url": "{{baseUrl}}/orders/:id" } },
        { "name": "Create Order", "request": { "method": "POST", "url": "{{baseUrl}}/orders" } },
        { "name": "Update Order", "request": { "method": "PATCH", "url": "{{baseUrl}}/orders/:id" } }
      ]
    },
    {
      "name": "Payments",
      "item": [
        { "name": "List Payments", "request": { "method": "GET", "url": "{{baseUrl}}/payments" } },
        { "name": "Create Payment", "request": { "method": "POST", "url": "{{baseUrl}}/payments" } },
        { "name": "Get Payment", "request": { "method": "GET", "url": "{{baseUrl}}/payments/:id" } }
      ]
    },
    {
      "name": "Shipments",
      "item": [
        { "name": "List Shipments", "request": { "method": "GET", "url": "{{baseUrl}}/shipments" } },
        { "name": "Create Shipment", "request": { "method": "POST", "url": "{{baseUrl}}/shipments" } },
        { "name": "Update Shipment", "request": { "method": "PATCH", "url": "{{baseUrl}}/shipments/:id" } }
      ]
    },
    {
      "name": "Returns",
      "item": [
        { "name": "List Returns", "request": { "method": "GET", "url": "{{baseUrl}}/returns" } },
        { "name": "Create Return", "request": { "method": "POST", "url": "{{baseUrl}}/returns" } },
        { "name": "Update Return", "request": { "method": "PATCH", "url": "{{baseUrl}}/returns/:id" } }
      ]
    },
    {
      "name": "Reviews",
      "item": [
        { "name": "List Reviews", "request": { "method": "GET", "url": "{{baseUrl}}/reviews" } },
        { "name": "Create Review", "request": { "method": "POST", "url": "{{baseUrl}}/reviews" } },
        { "name": "Update Review", "request": { "method": "PATCH", "url": "{{baseUrl}}/reviews/:id" } },
        { "name": "Delete Review", "request": { "method": "DELETE", "url": "{{baseUrl}}/reviews/:id" } }
      ]
    },
    {
      "name": "Inventory",
      "item": [
        { "name": "List Inventory", "request": { "method": "GET", "url": "{{baseUrl}}/inventory" } },
        { "name": "Update Inventory", "request": { "method": "PATCH", "url": "{{baseUrl}}/inventory/:warehouseId/:variantId" } }
      ]
    },
    {
      "name": "Coupons",
      "item": [
        { "name": "List Coupons", "request": { "method": "GET", "url": "{{baseUrl}}/coupons" } },
        { "name": "Create Coupon", "request": { "method": "POST", "url": "{{baseUrl}}/coupons" } }
      ]
    },
    {
      "name": "Promotions",
      "item": [
        { "name": "List Promotions", "request": { "method": "GET", "url": "{{baseUrl}}/promotions" } },
        { "name": "Create Promotion", "request": { "method": "POST", "url": "{{baseUrl}}/promotions" } }
      ]
    },
    {
      "name": "Audit Logs",
      "item": [
        { "name": "List Audit Logs", "request": { "method": "GET", "url": "{{baseUrl}}/audit-logs" } }
      ]
    },
    {
      "name": "Webhooks",
      "item": [
        { "name": "List Webhooks Events", "request": { "method": "GET", "url": "{{baseUrl}}/webhooks/events" } }
      ]
    }
  ],
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:3000/api" }
  ]
}
